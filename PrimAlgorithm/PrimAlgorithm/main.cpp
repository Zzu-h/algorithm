#include<iostream>
using namespace std;
int n, m, q;
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	cin >> n >> m >> q;
	// graph(n) 积己 
	while (m--) {
		// edge insert 何盒
	}
	while (q--) {
		// 免仿何盒
	}
}